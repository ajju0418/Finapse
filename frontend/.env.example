# Copy to .env.local. Never commit the real file.

# Base URL of the Finapse backend API.
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Must match `finapse.auth.refresh-cookie-name` on the backend.
NEXT_PUBLIC_REFRESH_COOKIE_NAME=finapse_refresh
